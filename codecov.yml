flags:
  unit:
    paths:
      - 'packages/vern-llm/**'
  integration:
    paths:
      - 'packages/vern-llm/**'
  vern-llm-redis-unit:
    paths:
      - 'packages/vern-llm-redis/**'
  vern-llm-redis-integration:
    paths:
      - 'packages/vern-llm-redis/**'

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0.5%
        paths:
          - 'packages/vern-llm/**'
      vern-llm-redis:
        target: auto
        threshold: 0.5%
        paths:
          - 'packages/vern-llm-redis/**'
    patch:
      default:
        target: 80%
        paths:
          - 'packages/vern-llm/**'
      vern-llm-redis:
        target: 80%
        paths:
          - 'packages/vern-llm-redis/**'

ignore:
  - '**/index.ts'
  - 'apps/**'
