---
title: Changelog
description: Release history for vern-llm
---

vern-llm uses [Changesets](https://github.com/changesets/changesets) for versioning. The full history lives in the package's `CHANGELOG.md`, generated automatically on each release.

## 0.2.0

**Minor Changes**

- Added a `tsconfig.base.json` which `tsconfig.json` now extends from.

<Callout>
  For the latest entries, check `packages/vern-llm/CHANGELOG.md` in the repo — this page reflects
  the version at time of writing and may lag behind the newest release.
</Callout>
