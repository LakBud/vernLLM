---
title: Getting Started
description: Install vern-llm and make your first call
---

## Install

<Tabs items={['pnpm', 'npm', 'yarn']}>
  <Tab value="pnpm">

```bash
pnpm add vern-llm
```

  </Tab>
  <Tab value="npm">

```bash
npm install vern-llm
```

  </Tab>
  <Tab value="yarn">

```bash
yarn add vern-llm
```

  </Tab>
</Tabs>

### Don't forget to install OpenAI too!

<Tabs items={['pnpm', 'npm', 'yarn']}>
  <Tab value="pnpm">

```bash
pnpm add openai
```

  </Tab>
  <Tab value="npm">

```bash
npm install openai
```

  </Tab>
  <Tab value="yarn">

```bash
yarn add openai
```

  </Tab>
</Tabs>

## Basic usage

```ts
import OpenAI from 'openai';
import { VernLLM } from 'vern-llm';

const openai = new OpenAI({ apiKey: process.env.OPENAI_API_KEY });

const llm = new VernLLM({
  client: openai,
  model: 'gpt-4o',
});

const parsed = await llm.call({
  systemPrompt: 'Return JSON: { "skills": string[] }', // JSON mode enabled by default
  userContent: 'Extract skills from: ...',
});
```

<Callout title="JSON mode">`jsonMode: false` returns the raw string without JSON parsing.</Callout>

`systemPrompt` is optional, omit it and no system message is sent at all:

```ts
const answer = await llm.call({ userContent: 'What is the capital of France?' });
```

## Per-call overrides

```ts
const llm = new VernLLM({ client: openai, model: 'gpt-4o-mini' });

await llm.call({
  systemPrompt: '...',
  userContent: '...',
  model: 'o3',
  reasoningEffort: 'high', // passed through as `reasoning_effort` for supported models
});
```
