{
  "name": "vern-llm-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "oxlint .",
    "lint:fix": "oxlint . --fix",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "build": "nx run-many -t build",
    "build:affected": "nx affected -t build",
    "dev": "nx run-many -t dev --parallel",
    "dev:docs": "nx dev vern-llm-docs",
    "typecheck": "nx run-many -t typecheck typecheck:test types:check",
    "typecheck:affected": "nx affected -t typecheck typecheck:test types:check",
    "test": "nx run-many -t test",
    "test:unit": "nx run-many -t test:unit",
    "test:unit:coverage": "nx run-many -t test:unit:coverage",
    "test:int": "nx run-many -t test:int",
    "test:int:coverage": "nx run-many -t test:int:coverage",
    "test:coverage": "nx run-many -t test:coverage",
    "test:affected": "nx affected -t test:unit:coverage test:int:coverage",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "update-changelog": "node scripts/update-changelog.ts",
    "prepare": "husky"
  },
  "lint-staged": {
    "{apps,packages}/**/*.{ts,tsx,js}": [
      "sh -c 'oxlint --fix \"$@\" || true' --",
      "oxfmt --write"
    ],
    ".changeset/*.md": [
      "oxfmt --write"
    ]
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.1",
    "@vitest/coverage-v8": "^4.1.11",
    "husky": "^9.1.7",
    "lint-staged": "^17.4.1",
    "nx": "^23.2.1",
    "oxfmt": "^0.65.0",
    "oxlint": "^1.80.0",
    "vitest": "^4.1.11"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "pnpm@11.25.0"
}
