{
  "name": "vern-llm-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "oxlint .",
    "lint:fix": "oxlint . --fix",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "typecheck": "pnpm --filter vern-llm-docs run types:check",
    "typecheck:test": "pnpm --filter vern-llm run typecheck:test",
    "test": "pnpm --filter vern-llm run test",
    "build": "pnpm --filter vern-llm-docs run build",
    "preview": "pnpm --filter vern-llm-docs run preview",
    "deploy": "pnpm --filter vern-llm-docs run deploy",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "changeset:publish": "changeset publish",
    "prepare": "husky"
  },
  "lint-staged": {
    "{apps,packages}/**/*.{ts,tsx,js}": [
      "sh -c 'oxlint --fix \"$@\" || true' --",
      "oxfmt --write"
    ],
    ".changeset/*.md": [
      "oxfmt --write"
    ]
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.8",
    "oxfmt": "^0.59.0",
    "oxlint": "^1.74.0"
  },
  "engines": {
    "node": ">=22.13"
  },
  "packageManager": "pnpm@11.15.0"
}
